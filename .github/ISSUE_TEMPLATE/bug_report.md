---
name: Bug report
about: Something isn't working
labels: bug
---

**What happened**

**What you expected**

**Steps to reproduce**
1.
2.

**Environment**
- Claude Code version (`claude --version`):
- `uv --version`:
- Python version (`python3 --version`):
- OS:
- Plugin version:

**Error output**
If the MCP server failed to start, paste the error from the Errors tab of `/plugin`.

```
```

**Style profile**
Do you have a `.gemini-design-profile.json`? If so, paste it (redact anything private).

```json
```
